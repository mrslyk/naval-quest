{
  "name": "naval-quest",
  "version": "0.2.0",
  "description": "Visual riddles from Naval's How to Get Rich tweetstorm — powered by Slyk",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run sync:rich && tsc && vite build",
    "sync:rich": "node scripts/sync-naval-rich.mjs",
    "sync:rewards": "node scripts/generate-reward-images.mjs",
    "sync:slyk": "node scripts/sync-slyk-tasks.mjs",
    "e2e:full": "node scripts/e2e-full-play.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "stripe": "^22.3.1"
  }
}
